# Repo-only files (not deployed to $HOME)
CLAUDE.md
README.md
docs/**
setup-op-token.sh
bootstrap.sh
.gitignore

{{ if ne .machine_type "mac-personal" }}
# mac-personal only
private_dot_ssh/**
{{ end }}

{{ if ne .machine_type "mac-personal" }}
# mac-personal only (ntfy LaunchAgent)
private_Library/**
{{ end }}
