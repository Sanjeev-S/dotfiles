# dotfiles

Bootstrap a fresh Ubuntu/Debian server with Claude Code.

## Usage

```bash
# SSH into the server
ssh hetzner-default

# Clone and run
git clone https://github.com/Sanjeev-S/dotfiles.git ~/dotfiles
bash ~/dotfiles/scripts/bootstrap.sh

# Authenticate (opens a URL → paste code back)
claude
```
